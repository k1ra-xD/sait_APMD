# 🎭 Инструкция по запуску сайта с собственной БД

## 📋 Что создано:

### Сервер (папка `server/`)
- `server.js` - Node.js сервер с API
- `database.json` - файл базы данных (все голоса хранятся здесь)
- `package.json` - конфигурация проекта

### Клиент (основная папка)
- `script-server.js` - новый скрипт для работы с вашим сервером
- Все остальные файлы (index.html, admin.html, results.html, styles.css)

---

## 🚀 Шаг 1: Запуск локально

### 1. Установите Node.js
Скачайте и установите: https://nodejs.org/ (выберите LTS версию)

### 2. Откройте терминал в папке `server`
```powershell
cd server
npm install
```

### 3. Запустите сервер
```powershell
npm start
```

Вы увидите:
```
🚀 Сервер запущен на http://localhost:3000
📊 База данных: D:\BAL_Prilojenie\server\database.json
```

### 4. Измените script.js
Замените содержимое `script.js` на содержимое `script-server.js`:
- Откройте `script-server.js`
- Скопируйте всё содержимое (Ctrl+A, Ctrl+C)
- Откройте `script.js`
- Вставьте (Ctrl+A, Ctrl+V)
- Сохраните (Ctrl+S)

### 5. Откройте сайт
Откройте `index.html` в браузере - всё работает с вашей БД!

---

## 🌐 Шаг 2: Загрузка на сервер (бесплатно)

### Вариант А: Render.com (рекомендую)

1. **Создайте аккаунт**: https://render.com (войдите через GitHub)

2. **Загрузите код на GitHub**:
```powershell
git add .
git commit -m "Add backend server"
git push
```

3. **Создайте Web Service на Render**:
   - Нажмите "New +" → "Web Service"
   - Подключите ваш репозиторий `sait_APMD`
   - Настройки:
     - **Name**: bal-voting-server
     - **Root Directory**: server
     - **Environment**: Node
     - **Build Command**: `npm install`
     - **Start Command**: `npm start`
     - **Plan**: Free
   - Нажмите "Create Web Service"

4. **Получите URL сервера**:
   После деплоя вы получите URL типа: `https://bal-voting-server.onrender.com`

5. **Обновите API_URL в script.js**:
```javascript
const API_URL = 'https://bal-voting-server.onrender.com/api';
```

6. **Загрузите обновлённый script.js на GitHub**:
```powershell
git add script.js
git commit -m "Update API URL"
git push
```

Готово! Ваш сайт работает с вашей БД на сервере! 🎉

### Вариант Б: Railway.app

1. Зарегистрируйтесь: https://railway.app
2. "New Project" → "Deploy from GitHub"
3. Выберите репозиторий `sait_APMD`
4. В настройках укажите Root Directory: `server`
5. Получите URL и обновите `API_URL` в script.js

---

## 📊 Как это работает:

1. **База данных** - файл `database.json` хранит все голоса
2. **Сервер** - обрабатывает запросы и обновляет БД
3. **Сайт** - отправляет запросы на сервер через API
4. **Синхронизация** - данные обновляются каждые 5 секунд автоматически

---

## 🔧 API который работает:

- `GET /api/couples` - получить все пары
- `POST /api/vote/:id` - проголосовать
- `GET /api/results-status` - проверить статус результатов
- `POST /api/results-status` - показать результаты (пароль: admin2025)
- `POST /api/reset` - сбросить голоса (пароль: admin2025)

---

## ❓ FAQ

**Q: Где хранятся голоса?**
A: В файле `server/database.json` - можете открыть и посмотреть

**Q: Как сбросить голоса?**
A: Откройте `database.json` и измените все `"votes": 0`

**Q: Сервер платный?**
A: Нет! Render и Railway дают бесплатный план

**Q: Что если сервер выключится?**
A: На бесплатном плане сервер засыпает через 15 минут без активности, но просыпается за 30 секунд при новом запросе

---

## 📞 Поддержка

Если что-то не работает, проверьте:
1. ✅ Node.js установлен (`node --version`)
2. ✅ Сервер запущен (`npm start` в папке server)
3. ✅ В консоли браузера нет ошибок (F12)
4. ✅ API_URL правильный в script.js
